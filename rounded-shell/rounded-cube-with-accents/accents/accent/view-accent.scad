use <accent.scad>

$fa = 1;
$fs = 0.4;

length=50;
width=20;
edge_radius=10;
radius=2;
offset=2;

accent(
    length=length,
    width=width,
    edge_radius=edge_radius,
    radius=radius,
    offset=2
);
