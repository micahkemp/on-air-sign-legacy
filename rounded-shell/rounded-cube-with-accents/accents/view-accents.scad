use <accents.scad>

length=50;
width=20;
height=20;
edge_radius=10;
radius=2;
count=3;
margin=3;

accents(
    length=length,
    width=width,
    height=height,
    edge_radius=edge_radius,
    radius=radius,
    count=count,
    margin=margin
);
